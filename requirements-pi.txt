# These are the requirements needed on the Raspberry Pi of the robot itself
# If you are using the docker image, you can just replace 'requirements.txt' with 'requirements-pi.txt' in the Dockerfile
# Otherwise, perform these installs manually
# For some of the packages, apt install might be needed instead of pip

-r requirements.txt

picamera2==0.3.34 ; sys_platform == "linux"
adafruit-blinka==9.0.4 ; sys_platform == "linux"
adafruit-circuitpython-servokit==1.3.23 ; sys_platform == "linux"